# H2 进阶：卡尔曼滤波 + 一键标定实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让距离估算更精确（焦距标定）、输出更平滑（卡尔曼滤波）、显示速度（靠近/远离箭头）。

**架构：** 新增卡尔曼滤波器类（per track_id 独立）；新增校准模块（WebSocket 接收标定命令 → 反算焦距 → 持久化）；距离计算模块改为动态加载焦距；前端 bbox 标签显示 `↑1.2m/s`；前端新增标定按钮。

**技术栈：** Python 3.14、numpy、pytest、React 18、TypeScript、Tailwind CSS。

---

## 文件结构与职责

- 创建：`backend/app/services/kalman_filter.py`
  - `KalmanFilter1D` 类：状态=[distance, velocity]，预测+更新+reset。
- 创建：`backend/tests/test_kalman_filter.py`
  - 恒定距离收敛、恒定速度跟踪、空观测不崩溃、重置。
- 创建：`backend/app/services/calibration.py`
  - 加载/保存焦距到 `data/calibration.json`；计算 `f_px`；默认值 700。
- 创建（可选）：`backend/tests/test_calibration.py`
  - 保存/加载正确，缺失返回默认值。
- 修改：`backend/app/services/distance_estimator.py`
  - `FOCAL_LENGTH_PX` 从常量改为从 `calibration.py` 动态加载。
- 修改：`backend/app/services/live_pipeline.py`
  - 新增 `_kf_registry: dict[int, KalmanFilter1D]`。
  - `_run_inference` 中集成卡尔曼滤波。
  - `_add_distances` 改为 `_add_distances_and_velocity`。
  - 输出 `velocity_ms`。
- 修改：`backend/app/api/live.py`
  - WebSocket 接收 `{"type":"calibrate","distance_m":2.0}` 时调用校准。
  - 将校准器引用传给 pipeline。
- 修改：`frontend/src/components/BboxOverlay.tsx`
  - label 显示 `↑1.2m/s` 或 `↓0.5m/s`。
- 修改：`frontend/src/pages/LivePreview.tsx`
  - 标定按钮 + 距离输入 + 标定状态反馈。

---

## 任务 1：创建卡尔曼滤波器模块

**文件：**
- 创建：`backend/app/services/kalman_filter.py`
- 创建：`backend/tests/test_kalman_filter.py`

- [ ] **步骤 1：编写失败测试**

创建 `backend/tests/test_kalman_filter.py`：

```python
import numpy as np
import pytest


@pytest.fixture
def kf():
    from app.services.kalman_filter import KalmanFilter1D
    return KalmanFilter1D(dt=0.2, process_noise=0.01, observation_noise=1.0)


def test_converges_to_constant_distance(kf):
    """恒定距离 5.0m，连续观测后 distance 应收敛到接近 5.0。"""
    for _ in range(20):
        kf.update(5.0)
    state = kf.get_state()
    assert state["distance"] == pytest.approx(5.0, abs=0.5)
    assert abs(state["velocity"]) < 0.3  # 静止目标速度应接近 0


def test_tracks_constant_velocity(kf):
    """匀速靠近（每帧距离减少 0.3m）应跟踪出速度约 1.5m/s（dt=0.2）。"""
    obs = [5.0, 4.7, 4.4, 4.1, 3.8, 3.5, 3.2, 2.9, 2.6, 2.3]
    for z in obs:
        kf.update(z)
    state = kf.get_state()
    assert state["velocity"] == pytest.approx(1.5, abs=0.5)


def test_empty_observation_does_not_crash(kf):
    """update(None) 应只做预测，不崩溃。"""
    kf.update(5.0)
    kf.update(None)  # 空观测
    state = kf.get_state()
    assert "distance" in state
    assert "velocity" in state


def test_reset_clears_state(kf):
    """reset() 应将状态清零。"""
    kf.update(5.0)
    kf.reset()
    state = kf.get_state()
    assert state["distance"] == 0.0
    assert state["velocity"] == 0.0
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd backend && uv run pytest tests/test_kalman_filter.py -v`
预期：FAIL，ImportError。

- [ ] **步骤 3：创建最小模块**

创建 `backend/app/services/kalman_filter.py`：

```python
"""1D 卡尔曼滤波器，用于平滑距离并估算速度。"""
```

- [ ] **步骤 4：实现卡尔曼滤波器**

写入 `backend/app/services/kalman_filter.py`：

```python
"""1D 卡尔曼滤波器，用于平滑距离并估算速度。

状态: [distance, velocity]^T
  distance  : 目标距离（米）
  velocity  : 目标速度（米/帧，正=靠近）

观测: distance_m（标量）
"""

import numpy as np


class KalmanFilter1D:
    def __init__(
        self,
        dt: float = 0.2,
        process_noise: float = 0.1,
        observation_noise: float = 1.0,
    ) -> None:
        self.dt = dt
        # 状态转移矩阵 F = [[1, dt], [0, 1]]
        self.F = np.array([[1.0, dt], [0.0, 1.0]], dtype=np.float64)
        # 观测矩阵 H = [1, 0]
        self.H = np.array([[1.0, 0.0]], dtype=np.float64)
        # 过程噪声协方差 Q
        self.Q = np.eye(2, dtype=np.float64) * process_noise
        # 观测噪声协方差 R
        self.R = np.eye(1, dtype=np.float64) * observation_noise
        # 状态向量 x = [distance, velocity]
        self.x = np.zeros((2, 1), dtype=np.float64)
        # 误差协方差 P
        self.P = np.eye(2, dtype=np.float64)

    def predict(self) -> None:
        self.x = self.F @ self.x
        self.P = self.F @ self.P @ self.F.T + self.Q

    def update(self, z: float | None) -> None:
        if z is not None:
            self.predict()
            z_arr = np.array([[z]], dtype=np.float64)
            y = z_arr - self.H @ self.x  # 残差
            S = self.H @ self.P @ self.H.T + self.R
            K = self.P @ self.H.T @ np.linalg.inv(S)
            self.x += K @ y
            self.P = (np.eye(2) - K @ self.H) @ self.P
        else:
            self.predict()

    def get_state(self) -> dict:
        return {
            "distance": round(float(self.x[0, 0]), 1),
            "velocity": round(float(self.x[1, 0]), 2),
        }

    def reset(self) -> None:
        self.x = np.zeros((2, 1), dtype=np.float64)
        self.P = np.eye(2, dtype=np.float64)
```

- [ ] **步骤 5：运行测试验证通过**

运行：`cd backend && uv run pytest tests/test_kalman_filter.py -v`
预期：4 passed。

- [ ] **步骤 6：Commit**

```bash
git add backend/app/services/kalman_filter.py backend/tests/test_kalman_filter.py
git commit -m "feat(H2): add Kalman filter for distance smoothing"
```

---

## 任务 2：创建校准模块并集成到 LivePipeline

**文件：**
- 创建：`backend/app/services/calibration.py`
- 修改：`backend/app/services/distance_estimator.py`
- 修改：`backend/app/services/live_pipeline.py`

- [ ] **步骤 1：创建 calibration.py**

创建 `backend/app/services/calibration.py`：

```python
"""焦距标定：保存/加载到 data/calibration.json，默认值 700。"""
import json
import os
from datetime import datetime, timezone
from pathlib import Path

CALIBRATION_PATH = Path("./data/calibration.json")
DEFAULT_FOCAL_LENGTH = 700


def _ensure_data_dir() -> None:
    CALIBRATION_PATH.parent.mkdir(parents=True, exist_ok=True)


def load_focal_length() -> int:
    if not CALIBRATION_PATH.exists():
        return DEFAULT_FOCAL_LENGTH
    try:
        with open(CALIBRATION_PATH) as f:
            data = json.load(f)
        return int(data.get("focal_length_px", DEFAULT_FOCAL_LENGTH))
    except (json.JSONDecodeError, OSError, ValueError):
        return DEFAULT_FOCAL_LENGTH


def save_focal_length(focal_length_px: int) -> None:
    _ensure_data_dir()
    with open(CALIBRATION_PATH, "w") as f:
        json.dump({
            "focal_length_px": focal_length_px,
            "calibrated_at": datetime.now(timezone.utc).isoformat(),
        }, f, indent=2)


def compute_focal_length(distance_m: float, h_norm: float, frame_height: int, known_height_m: float = 1.7) -> int:
    """从已知距离和 bbox 高度反算焦距。"""
    h_px = h_norm * frame_height
    if h_px <= 0:
        raise ValueError("bbox 高度为 0，无法标定")
    return round((distance_m * h_px) / known_height_m)
```

- [ ] **步骤 2：修改 distance_estimator.py**

把文件顶部的 `FOCAL_LENGTH_PX = 700` 常量替换为动态加载：

```python
FOCAL_LENGTH_PX = None
"""当前有效焦距。首次引用时从 data/calibration.json 加载。"""


def _get_focal_length() -> int:
    global FOCAL_LENGTH_PX
    if FOCAL_LENGTH_PX is None:
        from app.services.calibration import load_focal_length  # noqa: WPS433
        FOCAL_LENGTH_PX = load_focal_length()
    return FOCAL_LENGTH_PX
```

然后 `estimate_distance` 函数中把 `FOCAL_LENGTH_PX` 替换为 `_get_focal_length()`。

- [ ] **步骤 3：在 live_pipeline.py 中集成卡尔曼并输出 velocity_ms**

在 `LivePipeline.__init__` 中新增：

```python
        self._kf_registry: dict[int, KalmanFilter1D] = {}
```

在文件顶部 import 区添加：

```python
from app.services.kalman_filter import KalmanFilter1D
from app.services.calibration import compute_focal_length, save_focal_length
```

把 `_add_distances` 方法重命名为 `_add_distances_and_velocity` 并改为：

```python
    def _add_distances_and_velocity(self, frame_height: int) -> None:
        """为每个危险目标估算距离、卡尔曼平滑、输出速度。"""
        # 清理已消失目标的卡尔曼滤波
        visible_track_ids = {
            obj.get("track_id")
            for obj in self._last_objects
            if obj.get("label") in DANGER_LABELS
        }
        stale_ids = set(self._kf_registry.keys()) - visible_track_ids
        for tid in stale_ids:
            self._kf_registry.pop(tid, None)

        for obj in self._last_objects:
            label = obj.get("label", "")
            if label not in DANGER_LABELS:
                continue
            h_norm = obj.get("h", 0.0)
            d = estimate_distance(label, h_norm, frame_height)
            track_id = obj.get("track_id")
            if track_id is not None:
                if track_id not in self._kf_registry:
                    self._kf_registry[track_id] = KalmanFilter1D(dt=0.2)
                kf = self._kf_registry[track_id]
                kf.update(d)
                state = kf.get_state()
                obj["distance_m"] = state["distance"]
                obj["velocity_ms"] = round(state["velocity"] / 0.2, 1)  # 转 m/s
            else:
                if d is not None:
                    obj["distance_m"] = round(d, 1)
```

把 `_run_inference` 中的 `self._add_distances(h)` 改为 `self._add_distances_and_velocity(h)`。

- [ ] **步骤 4：运行测试验证**

运行：`cd backend && uv run pytest tests/test_kalman_filter.py tests/test_live_pipeline.py -v`
预期：全部通过。

- [ ] **步骤 5：Commit**

```bash
git add backend/app/services/calibration.py backend/app/services/distance_estimator.py backend/app/services/live_pipeline.py
git commit -m "feat(H2): add calibration and Kalman filter integration"
```

---

## 任务 3：WebSocket 标定消息处理

**文件：**
- 修改：`backend/app/api/live.py`
- 修改：`backend/tests/test_live_pipeline.py`

- [ ] **步骤 1：修改 live.py 处理标定消息**

在 `backend/app/api/live.py` 的 `while True` 循环前，添加标定消息处理变量。注意：当前 live.py 的 WebSocket 处理在后台线程中运行并行接收和发送，需要把标定命令注入到 pipeline 中。

最好的方式是在 LivePipeline 上加一个标定命令队列。在 `LivePipeline.__init__` 中新增：

```python
        self._calibrate_request: dict | None = None
```

在 `__iter__` 的每次迭代（yield 之前）检查并处理标定请求：

在 yield 之前添加：

```python
                # 处理标定请求
                cal_result = self._handle_calibrate_request()
                if cal_result:
                    yield cal_result
                    continue  # 标定消息单独发，不包含画面帧
```

新增方法（在 `stop()` 之前）：

```python
    def request_calibrate(self, distance_m: float) -> None:
        self._calibrate_request = {"distance_m": distance_m}

    def _handle_calibrate_request(self) -> dict | None:
        req = self._calibrate_request
        if req is None:
            return None
        self._calibrate_request = None

        # 使用 active target 计算焦距
        active_obj = None
        for obj in self._last_objects:
            if obj.get("is_active_target") and obj.get("track_id") is not None:
                active_obj = obj
                break
        if active_obj is None:
            return {"type": "error", "reason": "画面中没有检测到目标，请站在摄像头前再试"}

        label = active_obj.get("label", "person")
        known_heights = {"person": 1.70}
        known_height = known_heights.get(label, 1.7)
        h_norm = active_obj.get("h", 0.0)
        frame_height = (self._last_frame or {}).get("height", 480)

        try:
            from app.services.calibration import compute_focal_length, save_focal_length  # noqa: WPS433
            f_px = compute_focal_length(
                distance_m=req["distance_m"],
                h_norm=h_norm,
                frame_height=frame_height,
                known_height_m=known_height,
            )
            save_focal_length(f_px)
            # 清除距离模块的缓存，下次推理使用新焦距
            import app.services.distance_estimator as de  # noqa: WPS433
            de.FOCAL_LENGTH_PX = None
            return {"type": "calibrated", "focal_length_px": f_px}
        except ValueError as exc:
            return {"type": "error", "reason": str(exc)}
```

在 live.py 中，修改 WebSocket 循环支持接收消息：

在 `while True:` 块中，把纯发送改为双工接收。用 `asyncio.wait` 同时等待接收和发送：

```python
        try:
            recv_task = asyncio.create_task(websocket.receive_text())
            send_coro = None
            while True:
                # 等待接收或发送完成
                done, _ = await asyncio.wait(
                    [recv_task] if send_coro is None else [recv_task, asyncio.create_task(send_coro)],
                    return_when=asyncio.FIRST_COMPLETED,
                )
                if recv_task in done:
                    text = recv_task.result()
                    data = json.loads(text)
                    if data.get("type") == "calibrate":
                        pipeline.request_calibrate(data["distance_m"])
                    recv_task = asyncio.create_task(websocket.receive_text())
                if send_coro in done:
                    send_coro = None
```

但这样改动较大且复杂。简化方案：使用 `send_coro` 模式，每次先发送再检查接收。

更好的简化方案：在 live.py 的循环中，修改为使用 `asyncio.gather` 或者使用两个独立的任务。

实际上，最简单的方案是：**在现有的 `_next_msg` 异步生成器之后，单独启动一个接收任务**。

更简洁的方式——在 live.py 中，使用 `asyncio.create_task` 处理接收：

```python
        async def recv_loop():
            try:
                while True:
                    text = await websocket.receive_text()
                    data = json.loads(text)
                    if data.get("type") == "calibrate":
                        pipeline.request_calibrate(data["distance_m"])
            except WebSocketDisconnect:
                pass

        recv_task = asyncio.create_task(recv_loop())
```

在主发送循环前启动 `recv_loop`，在 `finally` 中取消它。

请按此完整方案修改 live.py。

- [ ] **步骤 2：运行测试验证**

运行：`cd backend && uv run pytest tests/test_live_pipeline.py -v`
预期：全部通过。

- [ ] **步骤 3：Commit**

```bash
git add backend/app/api/live.py backend/app/services/live_pipeline.py
git commit -m "feat(H2): add calibrate command via WebSocket"
```

---

## 任务 4：前端标定按钮 + 速度显示

**文件：**
- 修改：`frontend/src/pages/LivePreview.tsx`
- 修改：`frontend/src/components/BboxOverlay.tsx`

- [ ] **步骤 1：修改 BboxOverlay 显示速度**

在 `frontend/src/components/BboxOverlay.tsx` 中，找到 label 拼接部分，在 distText 之后添加：

```tsx
        const vel = obj.velocity_ms as number | undefined;
        const velText = typeof vel === 'number' && Math.abs(vel) >= 0.2
          ? ` ${vel > 0 ? '↑' : '↓'}${Math.abs(vel).toFixed(1)}m/s` : '';
```

然后把 label 行改为包含 velText：

```tsx
        const label = typeof trackId === 'number'
          ? `#${trackId} ${name} ${pct}${distText}${velText}`
          : `${name} ${pct}${distText}${velText}`;
```

- [ ] **步骤 2：修改 LivePreview 添加标定按钮**

在 `frontend/src/pages/LivePreview.tsx` 中：
1. 添加标定状态 state：`const [calibrating, setCalibrating] = useState<'idle' | 'done' | 'error'>('idle'); const [calFocal, setCalFocal] = useState<number | null>(null); const [calMsg, setCalMsg] = useState<string>(''); const calDistanceRef = useRef<HTMLInputElement>(null);`

2. 在 WebSocket 的 onmessage 中，判断返回消息是否带 type：

```tsx
    ws.onmessage = (event) => {
      try {
        const data = JSON.parse(event.data);
        if (data.type === 'calibrated') {
          setCalibrating('done');
          setCalFocal(data.focal_length_px);
          return;
        }
        if (data.type === 'error') {
          setCalibrating('error');
          setCalMsg(data.reason);
          return;
        }
        setMsg(data);
      } catch {
        // 忽略坏帧
      }
    };
```

3. 在组件内添加标定按钮区域：

```tsx
      {status === 'running' && (
        <div className="flex items-center gap-3 text-sm">
          <input
            ref={calDistanceRef}
            type="number"
            defaultValue={2.0}
            min={0.5}
            max={10}
            step={0.1}
            className="w-20 rounded border px-2 py-1 text-xs"
          />
          <button
            onClick={() => {
              const d = parseFloat(calDistanceRef.current?.value || '2.0');
              wsRef.current?.send(JSON.stringify({type: 'calibrate', distance_m: d}));
              setCalibrating('idle');
            }}
            className="rounded bg-cyan-600 px-3 py-1 text-xs text-white"
          >
            标定焦距
          </button>
          {calibrating === 'done' && calFocal && (
            <span className="text-green-600 text-xs">✅ 焦距已校准：{calFocal}px</span>
          )}
          {calibrating === 'error' && (
            <span className="text-red-600 text-xs">❌ {calMsg}</span>
          )}
        </div>
      )}
```

- [ ] **步骤 3：构建验证**

运行：`cd frontend && npm run build`
预期：构建通过。

- [ ] **步骤 4：Commit**

```bash
git add frontend/src/components/BboxOverlay.tsx frontend/src/pages/LivePreview.tsx
git commit -m "feat(H2): add calibration button and velocity display"
```

---

## 验证方案

### 单元测试
```bash
cd backend && uv run pytest tests/test_kalman_filter.py -v  # 4 passed
cd backend && uv run pytest tests/test_live_pipeline.py -v  # 含新测试
cd frontend && npm run build                                # 通过
```

### 手工验证
1. 启动后端+前端，打开实时预览。
2. 标定：输入距离 2.0 米，站到摄像头前 2 米处，点「标定焦距」。
3. 验证页面显示"✅ 焦距已校准：xxxpx"。
4. 人走近 bbox 标签出现 `↑X.Xm/s`。
5. 人走远标签出现 `↓X.Xm/s`。
6. 静止时没有速度箭头，不闪烁。
